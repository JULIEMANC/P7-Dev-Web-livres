const book = require("../models/book");
exports.createBook = async (req, res)=>{
   
    // try{
        console.log("okokokokokkkkkk")
        const {title}= req.body;
        // if( !title || !author || !imageUrl || !year ||!genre || !ratings ){
        //     return res
        //     .status(400)
        //     .json({message:"Veuillez remplir tout les champs du formulaire"});

        // }
    // }const Books = new Books({
console.log(title)
    // }
    //)
};
